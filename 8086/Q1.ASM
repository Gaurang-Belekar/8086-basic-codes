DATA SEGMENT
DIVIDEND DD 1000000H
DIVISOR DW 2000H
Q DD ?
REM DB ?
DATA ENDS
CODE SEGMENT
ASSUME CS:CODE,DS:DATA
START: MOV AX,DATA
       MOV DS,AX
       MOV AX,[SI]
       MOV DX,[SI+2]
       MOV BX,[1100]
       DIV BX
       MOV [1200],AX
       MOV [1300],DX
       MOV AH,2AC
       INT 21H
       CODE ENDS
       END START
